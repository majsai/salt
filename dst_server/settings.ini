[MISC]
CONSOLE_ENABLED = true
autocompiler_enabled = true


[NETWORK]
default_server_name = {{ pillar.get('dst:server_name', 'Server') }}
default_server_description = {{ pillar.get('dst:server_description', 'DST Server') }}
server_password = {{ pillar.get('dst:server_password', '') }}
max_players = 6

server_intention = {{ pillar.get('dst:server_intention', 'cooperative') }}
game_mode = {{ pillar.get('dst:game_mode', 'survival') }}
pvp = false

enable_autosaver = true
pause_when_empty = true
tick_rate = 60
